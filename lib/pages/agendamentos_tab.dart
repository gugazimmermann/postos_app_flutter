import 'package:flutter/material.dart';

class AgendamentosTab extends StatelessWidget {
  const AgendamentosTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text("Aqui ficarão os detalhes dos Agendamentos"));
  }
}
